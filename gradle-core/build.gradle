dependencies {
    implementation "${lib.'starter-thymeleaf'}"
    implementation "${lib.'jwt'}"

//    implementation "${lib.'starter-web'}"
    //没有引用mongo时不报错
//    implementation "${lib.'mongo'}"
    compileOnly "${lib.'mongo'}"
}

bootJar {
    enabled = false
}

jar {
    enabled = true
}
