dependencies {
    implementation "${lib.'starter-thymeleaf'}"
    implementation "${lib.'starter-aop'}"
    implementation "${lib.'rocketMqCommon'}"
    implementation "${lib.'rocketMqClient'}"
}

bootJar {
    enabled = false
}

jar {
    enabled = true
}
